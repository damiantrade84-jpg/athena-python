# --- Python / venv (read with pyproject.toml + .python-version) ---
# Target interpreter: 3.14 (.python-version). Supported range: >=3.11,<3.15 (pyproject.toml).
# If you change Python minor: remove .venv and recreate (py -3.14 -m venv .venv, then pip install -r requirements.txt).
# Do not reuse a .venv built with another Python version.
# pandas-ta removed: unused in production; pinned numba 0.61.2 blocked Python 3.14 installs.
flask==3.1.3
requests==2.32.5
openai>=1.0.0
anthropic>=0.49.0
yfinance==1.2.0
# vectorbt 1.0 requires pandas<3.0; keep the app venv aligned with Research Lab.
pandas==2.3.3
numpy==2.4.2
scipy==1.17.1
numba==0.65.1
llvmlite==0.47.0
vectorbt==1.0.0
# WebSocket feeds: Binance (!ticker@arr + microstructure), EODHD WS, Bybit WS.
# Pin 16.x — changing major versions changes defaults (ping/open timeouts) and can cause 1011 / handshake timeouts.
# Code assumes ping_interval=None on public streams where noted in athena.py / athena/datafeeds/*_ws.py.
# After recreating venv: pip install -r requirements.txt && python tools/check_ws_env.py
websockets==16.0
# Explicit CA bundle — reduces “works on one Python, SSL oddities on another” after venv swaps.
certifi>=2024.8.30
pyyaml==6.0.3
eodhd==1.0.32
python-dotenv==1.2.2
ccxt==4.5.42
pytest==8.4.2
# PTB 20.x breaks run_polling event loop on Python 3.14; 22.7+ required.
python-telegram-bot>=22.7
matplotlib>=3.8.0
mplfinance>=0.12.10b0
MetaTrader5>=5.0.0
