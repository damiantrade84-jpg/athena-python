# --- Python / venv (read with pyproject.toml + .python-version) ---
# Target interpreter for this repo: 3.13 (.python-version). Supported range: >=3.11 and <3.14 (pyproject.toml requires-python).
# If you change Python minor: remove .venv and recreate (py -3.13 -m venv .venv, then pip install -r requirements.txt).
# Do not reuse a .venv built with another Python version.
# Numba (via pandas-ta) has rejected install on 3.14 in verified pip runs here ("only versions >=3.10,<3.14 are supported").
flask==3.1.3
requests==2.32.5
openai>=1.0.0
yfinance==1.2.0
pandas==3.0.1
numpy>=2.2.0
# WebSocket feeds: Binance (!ticker@arr + microstructure), EODHD WS, Bybit WS.
# Pin 16.x — changing major versions changes defaults (ping/open timeouts) and can cause 1011 / handshake timeouts.
# Code assumes ping_interval=None on public streams where noted in athena.py / athena/datafeeds/*_ws.py.
# After recreating venv: pip install -r requirements.txt && python tools/check_ws_env.py
websockets==16.0
# Explicit CA bundle — reduces “works on one Python, SSL oddities on another” after venv swaps.
certifi>=2024.8.30
pyyaml==6.0.3
eodhd==1.0.32
python-dotenv==1.2.2
ccxt==4.5.42
pytest==8.4.2
scipy>=1.15.0
python-telegram-bot>=20.0
anthropic>=0.20.0
customtkinter>=5.2.2
darkdetect>=0.8.0
mplfinance>=0.12.10b0
pandas-ta>=0.3.14b
sounddevice>=0.5.0
soundfile>=0.12.0
duka>=0.2.0
MetaTrader5>=5.0.0
